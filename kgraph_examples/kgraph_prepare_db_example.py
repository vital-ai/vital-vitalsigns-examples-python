
def main():
    print('Hello World')
    # connect to graph db, check for existence of graph
    # create it if not present (not really needed, but keeps in sync with vector db)

    # connect to vector db,
    # using graph as namespace, check for classes of interest,
    # and insert classes if necessary


if __name__ == "__main__":
    main()
